import React from "react";
import { Typography } from "@mui/material";

const Error = () => {
  return (
    <Typography variant="h2" align="center">
      Error while fetching photos, please refresh the page and try again.
    </Typography>
  );
};

export default Error;
